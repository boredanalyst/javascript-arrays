// You can add elements to an array using .push()

const colors = ["red","orange","green"];

colors.push("pink");
console.log(colors);

// You can also add multiple elements with one push.
colors.push("yellow","gray");
console.log(colors);