// Convert arrays into strings with .toString()
// This will always concatenate arrays with a comma.

const colors = ["red","orange","yellow"];
const myColors = colors.toString();

console.log(myColors);