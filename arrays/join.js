// You can join elements of an array with .join();

const colors = ["red","orange","green"];

const myColors = colors.join(','); // This will concatenate all elements of the colors array with a comma.

console.log(myColors);