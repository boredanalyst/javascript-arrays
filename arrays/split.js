// You can split characters based on a delimiter to return an array.

const fruits = "apple,banana,guava";

const myFruits = fruits.split(','); // This will split the fruits variable using the ',' delimiter.

console.log(myFruits);