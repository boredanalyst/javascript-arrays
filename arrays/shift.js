// To remove the first item from an array, use .shift();

const colors = ["red","orange","yellow"];

console.log(colors.shift()); // This will return the first item of the array.

// This will return only the items that are left.
console.log(colors);