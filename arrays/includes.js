// you can use includes to see if an element is inside an array.
const colors = ["red","orange","blue"];
console.log(colors.includes("red")); // This will return true if the value is inside the array.