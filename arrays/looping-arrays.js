// Manipulate all elements of an array using for loops.

const colors = ["red","orange","yellow","green","blue","purple"];

for (const color of colors) {
    console.log(color.toUpperCase()); // This will return all colors in the array in upper case.
}